<UserControl x:Class="CellGanttChartEditor2.Controls.ChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctl="clr-namespace:CellGanttChartEditor2.Controls"
             Focusable="True" ClipToBounds="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ctl:ChartSurface x:Name="Surface" Grid.Row="0" Grid.Column="0"/>

        <!-- Sits over the surface so the inline row-header editor can be a real TextBox. -->
        <Canvas x:Name="Overlay" Grid.Row="0" Grid.Column="0" Background="{x:Null}"/>

        <ScrollBar x:Name="VScroll" Grid.Row="0" Grid.Column="1" Orientation="Vertical" Width="14"
                   Scroll="VScroll_Scroll"/>
        <ScrollBar x:Name="HScroll" Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Height="14"
                   Scroll="HScroll_Scroll"/>
    </Grid>
</UserControl>
